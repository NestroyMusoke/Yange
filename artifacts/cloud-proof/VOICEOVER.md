# Google Cloud proof voice-over

Use this as one continuous read over the 36.5-second clip:

> This is Yange running in production on Google Cloud, not a local simulation. Three Cloud Run revisions serve one hundred percent of traffic: a public edge, an IAM-protected worker, and an IAM-protected ADK steward. The runtime receipt confirms Firestore transactions, private Cloud Storage, Cloud Tasks, Pub/Sub, Gemini 3.5 Flash, multimodal Flash Lite, and Virtual Try-On. Gemini handles ambiguity, but deterministic domain policy owns availability, care safety, commits, and idempotency. In this fresh run, Yange validated 168 Google Weather periods, simulated two decision branches, committed two events, delivered one notification, and completed six durable checkpoints. Replaying the identical trigger returned the same Cloud Task with deduplicated true. Matching structured logs show the edge request, private worker, serving revisions, latencies, and Cloud Trace IDs. If one step fails, committed progress survives and resumes from its checkpoint. This is evidence built into the product.

The clip is intentionally silent so you can add this narration and your chosen music without cleaning source audio.
