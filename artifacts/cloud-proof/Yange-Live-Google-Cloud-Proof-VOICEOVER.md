# Yange live Google Cloud proof voiceover

Duration: 23 seconds

> This is Yange running on Google Cloud. Three Cloud Run services separate the public edge from authenticated workers, and the latest revision receives all production traffic. The live runtime reports Gemini, transactional Firestore, private Cloud Storage, Cloud Tasks and Pub/Sub. Finally, Cloud Logging ties real edge and worker requests to exact revisions, status codes, latency and trace IDs. This is the deployed system, not a simulation.

Keep the final sentence over the Cloud Logging frame. The clip is intentionally silent so it can sit beneath the main demo narration and music.
